How to configure git
====================

The git remote should be configured to the ssh url:

`git remote set-url origin git@github.com:almondtools/<name_of_project>.git`

The public ssh key must be registered with github
 